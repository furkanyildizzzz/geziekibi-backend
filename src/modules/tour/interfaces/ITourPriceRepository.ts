import { TourPrice } from 'orm/entities/tour/TourPrice';
import { IBaseRepository } from 'shared/interfaces/IBaseRepository';

export interface ITourPriceRepository extends IBaseRepository<TourPrice> {
}
